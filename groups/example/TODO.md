# To-do

<!-- Checklist items: - [ ] open, - [x] done.
     Optional deadline at the end of an item: (due: YYYY-MM-DD) -->
<!-- This file is mounted read-write into the agent's container but is NOT
     injected into its prompt — the agent opens it only when the conversation
     is about the list, so an idle list costs zero context. Ask the agent in
     chat to add, complete, or read items. Real groups get their own copy,
     seeded automatically on first use and gitignored (backed up to your
     private memory repo, never this public one). -->

- [ ] Example item — ask the agent to check this off
- [ ] Another example item with a deadline (due: 2026-09-01)

## Done

- [x] Set up the shared to-do list
